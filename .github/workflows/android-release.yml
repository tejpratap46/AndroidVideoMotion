name: Build and Attach ALL Debug APKs to Release

on:
  # Triggers the workflow when a new release is published
  release:
    types: [published]

jobs:
  build_and_attach:
    runs-on: ubuntu-latest
    
    # Only run this job if the release is not a pre-release or draft
    if: github.event.release.prerelease == false && github.event.release.draft == false

    steps:
      - name: ⚙️ Checkout Repository
        uses: actions/checkout@v4

      - name: ☕ Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: 📁 Set up Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Decode google-services.json
        run: echo "${{ secrets.GOOGLE_SERVICES_JSON }}" | openssl base64 -d > modules/lyrics-maker/google-services.json

      - name: 🔨 Build ALL Debug Artifacts (APKs & AARs)
        # This command runs the 'assembleDebug' task across all subprojects/modules
        run: |
          ./gradlew assembleDebug

      # NOTE: This step is fixed to correctly output multi-line file paths for the next action.
      - name: 📌 Define ALL Artifact Paths
        id: artifact_paths
        run: |
          echo "🔎 Searching for APKs and AARs..."
          
          # Find all debug APKs and AARs in standard output directories
          ARTIFACT_FILES=$(find . \( -path '*/build/outputs/apk/debug/*-debug.apk' -o -path '*/build/outputs/aar/*-debug.aar' \) -type f)
          
          if [ -z "$ARTIFACT_FILES" ]; then
            echo "::warning::No debug APKs or AARs found in the standard build paths."
            echo "ARTIFACT_FILES=" >> $GITHUB_OUTPUT
          else
            echo "✅ Found the following artifacts:"
            echo "$ARTIFACT_FILES"
            
            # Use the EOF delimiter to output a multi-line string with true newlines
            echo "ARTIFACT_FILES<<EOF" >> $GITHUB_OUTPUT
            echo "$ARTIFACT_FILES" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          fi

      - name: 📤 Upload ALL Artifacts to Release Assets
        # Using the recommended v2 of the action
        uses: softprops/action-gh-release@v2
        if: steps.artifact_paths.outputs.ARTIFACT_FILES != '' # Only run if artifacts were found
        with:
          # The 'files' input receives the newline-separated list of paths
          files: ${{ steps.artifact_paths.outputs.ARTIFACT_FILES }}
          token: ${{ secrets.GITHUB_TOKEN }}
          # Added based on your previous logs to allow re-running and replacing files
          overwrite_files: true
